[package]
name = "wireguard_viz"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
async-stream = "0.3"
serde = { version = "1", features = ["derive"] }
